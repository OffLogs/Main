apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.images.api.serviceName }}
spec:
  type: ClusterIP
  selector: 
    app: {{ .Values.images.api.deploymentName }}
  ports:
    - port: 80
      protocol: TCP
